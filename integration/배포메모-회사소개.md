# 회사소개 배포 메모 — 2026-09-10 중단, 이어서 할 것

## 현재 상황
- 알드라이브(FTP)로 **운영서버의 회사소개 JSP를 직접 수정**함
  - 경로: `/tomcat/webapps/ROOT/WEB-INF/jsp/company/company.jsp`
  - 서버 파일 수정시각: 2026-09-10 18:30
- 그런데 본사이트(new.honeymoonresort.co.kr)에 **반영이 안 됨**
- ※ 서버에서 뭘 고쳤는지 내용은 이 레포에 없음 (레포 `integration/company.jsp`는 커밋 f11d0e2 이후 변경 없음)

## 반영 안 되는 원인 (거의 확정)
운영 톰캣 Jasper가 `development=false` → `.jsp` 수정시각을 재확인 안 함.
`work/`에 이미 컴파일된 servlet(`.class`)을 계속 서빙해서, FTP로 덮어써도 재컴파일이 안 됨.

## 내일 할 것 (택1)
1. **톰캣 재시작** (제일 확실)
2. **ROOT 컨텍스트만 리로드**: `/manager/html`에서 ROOT `Reload`, 또는 서버 `WEB-INF/web.xml`을 한 번 저장(touch)
3. 서버 `tomcat/work/Catalina/localhost/ROOT/org/apache/jsp/WEB_002dINF/jsp/company/company_jsp.class`(및 `.java`) 삭제 후 새로고침

## 그 외 체크
- **이미지/영상을 바꾼 변경이면**: jsp 에셋 경로가 서버경로(`../new_ver/img/company-intro/...`)라 그 파일도 서버에 올려야 반영됨
- 앞단 CDN/프록시 캐시 가능성 → `Ctrl+F5`, URL에 `?v=1` 붙여 확인
- 모바일(`webapps/m`)은 별도 코드 — PC 회사소개면 위 경로가 맞음

## 집에서 이어서 할 경우
- 위 작업은 **서버 접속(FTP / 톰캣 매니저)이 가능해야** 함
- 서버에서 고친 내용을 레포에도 남기려면: 서버 `company.jsp`를 내려받아 레포 `integration/company.jsp`에 병합
